[project]
name = "transcribe-modal-gpu"
version = "0.1.0"
description = "GPU-side Modal app for Whisper transcription and speaker diarization"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.0.3",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
